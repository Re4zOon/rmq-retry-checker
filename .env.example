# Example environment variables for RabbitMQ Retry Checker
# Copy this to .env and customize for your environment
#
# Configuration precedence:
#   1. Command-line arguments
#   2. Config file (--config)
#   3. Environment variables (this file)
#   4. Default values

# RabbitMQ Connection Settings
RMQ_HOST=localhost
RMQ_PORT=5672
RMQ_USERNAME=guest
RMQ_PASSWORD=guest
RMQ_VHOST=/

# Queue Settings
DLQ_NAME=my_dlq
TARGET_QUEUE=permanent_failure_queue
MAX_RETRY_COUNT=3

# Optional: Use SSL/TLS
# RMQ_USE_SSL=true

